# Example portfolio PDF

Sample portfolio PDF used on the brand docs (implementations/portfolio-pdf.html). Generated from the same template as release portfolios.

To refresh: run `node scripts/generate-portfolio-pdf.mjs --slug andre-lademann` (or `node scripts/generate-staff-assets.mjs`), then copy `release-assets/staff/portfolios/portfolio-andre-lademann.pdf` here.
